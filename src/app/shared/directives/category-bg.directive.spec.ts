import { CategoryBgDirective } from './category-bg.directive';

describe('CategoryBgDirective', () => {
    it('should create an instance', () => {
        const directive = new CategoryBgDirective();
        expect(directive).toBeTruthy();
    });
});
